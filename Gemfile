source "https://rubygems.org"

# Reproduit l'environnement de build de GitHub Pages en local :
#   bundle install
#   bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick"

# Windows / JRuby
gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw, :jruby]
